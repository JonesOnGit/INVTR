<div id="inviter">
	<p> Report <%= @email %> for <%= @invite.name %></p>
	<%= render 'details', invite: @invite %>
	<p>This invite has been marked as reported. If you would like to leave an explanation, please do so below</p>
	<%= render 'report_form', invite: @invite, url: "/invites/#{@invite.id}/report" %>
</div>
